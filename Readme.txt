In the cmd, the program can be run with:
Example: python run_experiments.py --instance instances/exp0.txt --solver MACBS

MACBS solver was added as final project

The experiment maps we used are located in exp folder

EPEA* is implemented in single_agent_path_finding.py file

In the MA_CBS.py file, you can adjust B(boundary) value